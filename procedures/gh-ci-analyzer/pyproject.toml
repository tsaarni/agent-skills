[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "gh-ci-analyzer"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = ["jinja2"]

[tool.setuptools]
packages = ["analyzer"]

[project.scripts]
analyze-pr-failures = "analyzer.cli:main"
